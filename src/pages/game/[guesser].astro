---
import Layout from "../../layouts/Layout.astro";
import PokemonGuesser from "../../components/PokemonGuesser.jsx";
import { getPokemonsByIdOrName } from "../../lib/controllers/pokemonController";

const randomNumber = Math.floor(Math.random() * 1025) + 1;
const pokemon = await getPokemonsByIdOrName(randomNumber.toString());
const pokemonName = pokemon?.basicData.name;

const img = pokemon?.basicData.image;
---

<Layout title="Adivina el Pokémon">
    <PokemonGuesser pokemonName={pokemonName} pokemonImage={img} client:only/>
</Layout>


